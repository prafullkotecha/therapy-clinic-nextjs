/**
 * Application-wide constants
 */

/**
 * Token value used for dev bypass authentication.
 * This token is used instead of a real OAuth token when DEV_BYPASS_AUTH is enabled.
 */
export const DEV_BYPASS_TOKEN = 'dev-bypass-token';
